server:
  port: 11300

spring:
  application:
    name: batch-app


--- # local
spring:
  config:
    activate:
      on-profile: local
    import:
      - classpath:anything-common.yml

--- # dev
spring:
  config:
    activate:
      on-profile: dev